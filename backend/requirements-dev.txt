-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
ruff==0.8.6
pyright==1.1.391

# numpy is a DEV-ONLY dependency: it generates deterministic synthetic fixture
# embeddings in scripts/seed_synthetic.py. It must never enter requirements.txt.
numpy==2.2.1
