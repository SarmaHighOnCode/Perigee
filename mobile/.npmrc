# Carried over from testing/testcamera, where it was found the hard way:
# native Android CMake paths exceed the Windows path limit under pnpm's deep
# virtual-store layout. Hoisting keeps them short enough to build.
node-linker=hoisted

# Metro does not follow symlinks reliably across workspace packages.
shamefully-hoist=true
